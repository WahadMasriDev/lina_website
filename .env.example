# Copy this file to .env (never commit the real .env — it's already in .gitignore)
# and fill in the real values. Used by api.php via getenv().
DB_HOST=localhost
DB_NAME=your_real_db_name
DB_USER=your_real_db_user
DB_PASS=your_real_db_password
